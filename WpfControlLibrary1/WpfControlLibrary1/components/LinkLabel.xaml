<UserControl x:Class="WpfControlLibrary1.LinkLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             mc:Ignorable="d" >
        <Grid Width="140" Height="40">
        <Path Stroke="#ACACAC" StrokeThickness="2" Fill="#F0F0F0" 
              Data="M 0,0 120,0, 140,20 120,40 0,40 0,0"/>
        <Label Width="100" Height="40" FontSize="14" FontFamily="Lucida Sans UI"
               Content="{Binding Path=LabelContent, FallbackValue=Hello}" />
    </Grid>
    </UserControl>
